import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_tcc/app/modules/registros/page/procedimento_medico/procedimento_medico_page.dart';

void main() {
  testWidgets('ProcedimentoMedicoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProcedimentoMedicoPage(title: 'ProcedimentoMedico')));
    //  final titleFinder = find.text('ProcedimentoMedico');
    //  expect(titleFinder, findsOneWidget);
  });
}
