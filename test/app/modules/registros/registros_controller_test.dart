import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:app_tcc/app/modules/registros/registros_controller.dart';
import 'package:app_tcc/app/modules/registros/registros_module.dart';

void main() {
  initModule(RegistrosModule());
  // RegistrosController registros;
  //
  setUp(() {
    //     registros = RegistrosModule.to.get<RegistrosController>();
  });

  group('RegistrosController Test', () {
    //   test("First Test", () {
    //     expect(registros, isInstanceOf<RegistrosController>());
    //   });

    //   test("Set Value", () {
    //     expect(registros.value, equals(0));
    //     registros.increment();
    //     expect(registros.value, equals(1));
    //   });
  });
}
