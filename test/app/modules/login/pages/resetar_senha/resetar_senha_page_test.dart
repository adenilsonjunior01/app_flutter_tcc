import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_tcc/app/modules/login/pages/resetar_senha/resetar_senha_page.dart';

void main() {
  testWidgets('ResetarSenhaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ResetarSenhaPage(title: 'ResetarSenha')));
    //  final titleFinder = find.text('ResetarSenha');
    //  expect(titleFinder, findsOneWidget);
  });
}
