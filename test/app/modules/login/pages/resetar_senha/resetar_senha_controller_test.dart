import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:app_tcc/app/modules/login/pages/resetar_senha/resetar_senha_controller.dart';
import 'package:app_tcc/app/modules/login/login_module.dart';

void main() {
  initModule(LoginModule());
  // ResetarSenhaController resetarsenha;
  //
  setUp(() {
    //     resetarsenha = LoginModule.to.get<ResetarSenhaController>();
  });

  group('ResetarSenhaController Test', () {
    //   test("First Test", () {
    //     expect(resetarsenha, isInstanceOf<ResetarSenhaController>());
    //   });

    //   test("Set Value", () {
    //     expect(resetarsenha.value, equals(0));
    //     resetarsenha.increment();
    //     expect(resetarsenha.value, equals(1));
    //   });
  });
}
